# Local config / secrets — never commit
config.json
config.local.json
*.local.json

# Local env file (use env.example.txt as the reference)
env.txt

# Throwaway / scratch scripts
__*.ps1

# Go TUI build output (shipped as a GitHub Release asset, never committed)
dist/
*.exe

# debug logs
*.log
